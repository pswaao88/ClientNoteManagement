# 아빠 앱 실기기 테스트 및 배포 메모

## 1. APK 위치

- 디버그 APK: `C:\Users\Public\Documents\ESTsoft\CreatorTemp\ipadress_build\app\build\outputs\apk\debug\app-debug.apk`

## 2. 설치 전 체크사항

- 안드로이드 기기에서 `설정 > 개발자 옵션 > USB 디버깅`을 켠다.
- USB 연결 후 PC에서 기기가 `adb devices`로 보이는지 확인한다.
- 실기기 저장공간이 충분한지 확인한다.
- 기존에 설치된 같은 앱이 있으면 충돌 방지를 위해 제거 후 설치한다.
- 테스트는 가능한 실제 설치 직후, 초기 데이터 상태에서 진행한다.
- 원본 프로젝트 경로는 한글이 포함되어 있으므로, 빌드는 ASCII 경로 복사본에서 진행하는 것이 안전하다.

## 3. 실기기 테스트 체크리스트

### 3.1 앱 실행

- 앱이 정상적으로 실행되는지 확인한다.
- 첫 화면에서 거래처 목록이 뜨는지 확인한다.
- 앱 재실행 후 데이터가 유지되는지 확인한다.

### 3.2 거래처 CRUD

- 거래처를 새로 등록할 수 있는지 확인한다.
- 거래처를 수정했을 때 목록과 상세 화면에 반영되는지 확인한다.
- 거래처 삭제 시 삭제 확인 다이얼로그가 뜨는지 확인한다.
- 삭제 후 목록에서 즉시 사라지는지 확인한다.

### 3.3 과 CRUD

- 거래처 상세에서 과를 추가할 수 있는지 확인한다.
- 과를 수정했을 때 반영되는지 확인한다.
- 과 삭제 후 해당 거래처 목록에서 제거되는지 확인한다.
- 다른 거래처의 과와 섞이지 않는지 확인한다.

### 3.4 기기 CRUD

- 과 상세에서 기기를 추가할 수 있는지 확인한다.
- 기기번호, 모델명, IP, ID, PW, 메모가 정상 저장되는지 확인한다.
- 기기 수정이 정상 반영되는지 확인한다.
- 기기 삭제 시 확인 다이얼로그가 뜨고 삭제되는지 확인한다.

### 3.5 PC CRUD

- 과 상세에서 PC를 추가할 수 있는지 확인한다.
- 자리 뒷번호, IP, ID, PW, 메모가 정상 저장되는지 확인한다.
- PC 수정이 정상 반영되는지 확인한다.
- PC 삭제 시 확인 다이얼로그가 뜨고 삭제되는지 확인한다.

### 3.6 검색

- 거래처 검색이 정상 동작하는지 확인한다.
- 과 검색이 정상 동작하는지 확인한다.
- 기기 검색이 기기번호, 모델명, IP 기준으로 동작하는지 확인한다.
- PC 검색이 자리 뒷번호, IP 기준으로 동작하는지 확인한다.

### 3.7 비밀번호 표시

- 기기와 PC 입력 화면에서 비밀번호가 기본적으로 가려지는지 확인한다.
- 비밀번호 보기 버튼을 눌렀을 때 표시가 바뀌는지 확인한다.
- 다시 누르면 가려지는지 확인한다.

## 4. 배포 시 주의사항

- 이 앱은 서버가 없으므로 데이터는 기기 내부에만 저장된다.
- 앱 삭제 시 로컬 데이터가 함께 사라진다.
- 백업 기능이 아직 없으므로 중요한 정보는 별도 보관이 필요하다.
- 비밀번호와 접속 정보가 포함되므로 타인에게 APK를 공유할 때 주의한다.
- 원본 프로젝트 경로에 한글이 포함되어 있으므로, 빌드나 재검증은 ASCII 경로에서 하는 것이 안전하다.
- 배포 전에는 반드시 실기기에서 CRUD와 검색을 한 번씩 확인한다.

## 5. 최종 배포 판단 기준

- 거래처 등록/수정/삭제가 된다.
- 과 등록/수정/삭제가 된다.
- 기기 등록/수정/삭제가 된다.
- PC 등록/수정/삭제가 된다.
- 검색이 정상 동작한다.
- 비밀번호가 기본 가림 처리된다.
- 실기기에서 앱이 정상 실행된다.

위 항목이 모두 통과하면 현재 버전은 실사용 가능한 1차 배포본으로 판단한다.
