# 아빠 앱 안드로이드 오프라인 단계별 개발 계획

## 1. 전제 조건

이번 앱은 아래 조건으로 개발한다.

- 플랫폼: 안드로이드 전용
- 서버: 없음
- 데이터 저장: 기기 내부 로컬 저장
- 목적: 거래처 > 과 > 기기/PC 정보 조회 및 관리
- 개발 목표: 오늘 안에 MVP 완성 가능한 구조로 설계

이 조건이면 웹 서버, 관리자 페이지, 회원 시스템 없이도 실사용 가능한 앱을 만들 수 있다.

## 2. 개발 방향 요약

이번 앱은 복잡한 서비스가 아니라 "현장에서 빠르게 조회하고 수정하는 업무용 도구"로 봐야 한다.

따라서 개발 방향은 아래처럼 잡는 것이 맞다.

- UI는 단순해야 한다.
- 입력보다 조회 속도가 중요하다.
- 서버 없이도 데이터가 남아야 한다.
- CRUD 중심으로 먼저 완성해야 한다.
- APK 설치 후 바로 사용할 수 있어야 한다.

## 3. 권장 기술 스택

안드로이드 전용 오프라인 앱 기준으로 가장 현실적인 선택은 아래 조합이다.

- 언어: Kotlin
- UI: Jetpack Compose
- 로컬 DB: Room
- 상태 관리: ViewModel
- 화면 이동: Navigation Compose
- 간단 설정 저장: DataStore

### 선택 이유

- Kotlin은 현재 안드로이드 기본 개발 언어다.
- Jetpack Compose는 화면 개발 속도가 빠르다.
- Room은 서버 없이도 거래처/과/기기/PC 데이터를 안정적으로 저장할 수 있다.
- ViewModel은 화면 회전이나 재진입 시 상태 관리가 편하다.
- DataStore는 앱 잠금 여부, 설정값 같은 간단한 저장에 적합하다.

## 4. MVP 범위

오늘 안에 완성하려면 범위를 강하게 제한해야 한다.

### MVP에 포함

- 거래처 목록 조회
- 거래처 등록 / 수정 / 삭제
- 거래처별 과 목록 조회
- 과 등록 / 수정 / 삭제
- 과별 기기 목록 조회
- 기기 등록 / 수정 / 삭제
- 과별 PC 목록 조회
- PC 등록 / 수정 / 삭제
- 거래처명 / 과명 / 기기번호 / 모델명 / IP / 자리 뒷번호 검색
- 앱 재실행 후 데이터 유지

### MVP에서 제외

- 서버 연동
- 회원가입 / 로그인
- 클라우드 동기화
- 여러 사용자 권한 분리
- 사진 첨부
- 엑셀 내보내기
- 백업/복원
- 알림 기능

## 5. 추천 앱 구조

앱의 정보 구조는 고정한다.

`거래처 > 과 > 기기 / PC`

### 화면 구조

1. 홈 화면
- 거래처 목록
- 거래처 검색
- 거래처 추가 버튼

2. 거래처 상세 화면
- 거래처 기본 정보
- 해당 거래처의 과 목록
- 과 추가 버튼

3. 과 상세 화면
- 상단: 과 정보
- 탭 또는 섹션 1: 기기 목록
- 탭 또는 섹션 2: PC 목록

4. 기기 등록/수정 화면
- 기기번호
- 모델명
- IP 주소
- ID
- PW
- 비고

5. PC 등록/수정 화면
- 자리 뒷번호
- IP 주소
- ID
- PW
- 비고

## 6. 추천 데이터 구조

서버가 없으므로 Room 테이블 설계가 가장 중요하다.

### Client

- id
- name
- address
- managerName
- phone
- memo
- createdAt
- updatedAt

### Department

- id
- clientId
- name
- memo
- createdAt
- updatedAt

### Device

- id
- departmentId
- deviceNumber
- modelName
- ipAddress
- loginId
- loginPassword
- memo
- createdAt
- updatedAt

### PcEntry

- id
- departmentId
- seatSuffix
- ipAddress
- loginId
- loginPassword
- memo
- createdAt
- updatedAt

## 7. 단계별 개발 계획

## 7.1 1단계: 요구사항 확정

목표:

- 오늘 만들 기능과 제외 기능을 확정한다.

작업:

- 거래처/과/기기/PC 구조 고정
- 각 항목 필드 확정
- 검색 범위 확정
- 삭제 정책 확정

산출물:

- 요구사항 문서
- 필드 목록 문서
- MVP 범위 문서

완료 기준:

- 어떤 화면이 필요한지 확정
- 어떤 데이터가 저장되는지 확정
- 오늘 안에 구현할 범위가 명확함

리스크:

- 기능이 계속 추가되면 일정이 무너진다.

## 7.2 2단계: 기술 구조 확정

목표:

- 개발 중 기술 선택으로 시간을 낭비하지 않도록 구조를 고정한다.

작업:

- Kotlin 사용 확정
- Compose 사용 확정
- Room DB 사용 확정
- ViewModel + Navigation Compose 구조 확정
- DataStore 사용 여부 확정

산출물:

- 기술 선택 문서
- 패키지 구조 초안

완료 기준:

- 프로젝트 초기 설정 방향이 고정됨

리스크:

- 여기서 흔들리면 이후 구현 속도가 크게 떨어진다.

## 7.3 3단계: 화면 흐름 설계

목표:

- 실제 사용 순서에 맞는 화면 이동을 먼저 정한다.

작업:

- 홈 화면 정의
- 거래처 상세 화면 정의
- 과 상세 화면 정의
- 기기 입력 화면 정의
- PC 입력 화면 정의
- 상세/편집 분리 여부 결정

산출물:

- 화면 흐름도
- 화면 목록
- 입력 항목 정의

완료 기준:

- 뒤로 가기와 추가/수정 흐름이 명확함

리스크:

- 화면 구조 없이 바로 개발하면 재작업이 많아진다.

## 7.4 4단계: 데이터 모델링

목표:

- Room 기반 로컬 DB 구조를 확정한다.

작업:

- Entity 정의
- DAO 메서드 정의
- 관계 구조 정의
- 검색 쿼리 초안 정의

산출물:

- Entity 초안
- DAO 목록
- DB 설계 문서

완료 기준:

- 거래처, 과, 기기, PC CRUD가 모두 가능한 상태

리스크:

- 테이블 관계가 잘못되면 삭제/수정 시 문제 발생

## 7.5 5단계: 안드로이드 프로젝트 초기 세팅

목표:

- 바로 기능을 붙일 수 있는 기본 앱 골격을 만든다.

작업:

- Android 프로젝트 생성
- Compose 설정
- Navigation 설정
- Room 의존성 추가
- 기본 패키지 구조 생성

추천 패키지 구조:

- `ui`
- `navigation`
- `data/local`
- `data/repository`
- `feature/client`
- `feature/department`
- `feature/device`
- `feature/pc`
- `common`

산출물:

- 빌드 가능한 초기 프로젝트

완료 기준:

- 앱이 실행됨
- 기본 화면 이동 가능

리스크:

- Gradle 설정 문제로 초기 시간이 많이 소모될 수 있음

## 7.6 6단계: 거래처 CRUD 구현

목표:

- 가장 상위 단위인 거래처 관리 기능을 먼저 완성한다.

작업:

- 거래처 목록 UI
- 거래처 추가 화면
- 거래처 수정 화면
- 거래처 삭제 처리
- 거래처 검색 기능

산출물:

- 거래처 기능 완성

완료 기준:

- 거래처 생성/수정/삭제가 모두 정상 동작
- 목록 반영이 즉시 됨

리스크:

- 입력 검증이 없으면 빈 데이터가 저장될 수 있음

## 7.7 7단계: 과 CRUD 구현

목표:

- 거래처 내부 과 구조를 완성한다.

작업:

- 거래처 상세 화면에서 과 목록 표시
- 과 추가/수정/삭제
- 거래처와 과 연결 유지
- 과명 검색 또는 정렬 처리

산출물:

- 과 관리 기능 완성

완료 기준:

- 특정 거래처에 속한 과만 정확히 보임

리스크:

- 거래처 ID 연결이 잘못되면 데이터가 섞일 수 있음

## 7.8 8단계: 기기 CRUD 구현

목표:

- 과 단위 대여 기기 정보 관리 기능을 구현한다.

작업:

- 기기 목록 UI
- 기기 추가/수정/삭제
- 기기 상세 보기
- 기기번호, 모델명, IP 검색

산출물:

- 기기 관리 기능 완성

완료 기준:

- 특정 과에 속한 기기만 표시됨
- IP, ID, PW 조회 가능

리스크:

- 기기번호와 모델명 용도가 헷갈릴 수 있으므로 라벨을 명확히 해야 함

## 7.9 9단계: PC CRUD 구현

목표:

- 과 단위 PC 자리 정보 관리 기능을 구현한다.

작업:

- PC 목록 UI
- PC 추가/수정/삭제
- 자리 뒷번호와 IP 1:1 매칭 표시
- 뒷번호, IP 검색

산출물:

- PC 관리 기능 완성

완료 기준:

- 특정 과에 속한 PC만 표시됨
- 자리 뒷번호 기준으로 빠른 조회 가능

리스크:

- 자리 뒷번호 중복 정책을 미리 정하지 않으면 관리가 꼬일 수 있음

## 7.10 10단계: 검색 기능 통합

목표:

- 현장에서 필요한 정보를 빠르게 찾을 수 있게 한다.

작업:

- 거래처 검색
- 과 검색
- 기기번호/모델명/IP 검색
- 자리 뒷번호/IP 검색

산출물:

- 검색 가능한 목록 화면

완료 기준:

- 입력 후 바로 필터링 결과가 반영됨

리스크:

- 모든 검색을 한 화면에서 한 번에 처리하면 복잡도가 커질 수 있음

## 7.11 11단계: UX 및 최소 보안 보강

목표:

- 실사용 중 불편함과 민감정보 노출을 줄인다.

작업:

- 비밀번호 기본 가림 처리
- 삭제 확인 다이얼로그
- 빈 값 저장 방지
- 잘못된 IP 형식 최소 검증
- 저장/수정 성공 메시지 처리

산출물:

- 안정된 입력 UX
- 최소 보안 대응

완료 기준:

- 실수 입력이 줄어듦
- 비밀번호가 무조건 노출되지 않음

리스크:

- 보안 고도화까지 하려면 오늘 일정에서 벗어날 수 있음

## 7.12 12단계: 테스트 및 APK 배포

목표:

- 실제 설치 가능한 MVP로 마무리한다.

작업:

- CRUD 전체 테스트
- 거래처-과-기기/PC 연결 테스트
- 검색 테스트
- 삭제 시 반영 테스트
- 실제 안드로이드 기기 설치 테스트
- APK 빌드

산출물:

- 테스트 체크리스트
- 설치 가능한 APK

완료 기준:

- 주요 기능이 실제 기기에서 문제없이 동작

리스크:

- 에뮬레이터와 실제 기기 UI가 다를 수 있음

## 8. 서브 에이전트 도입 방식

여기서 말하는 서브 에이전트는 AI 작업 분담 기준이기도 하고, 사람이 나눠서 일할 때도 그대로 적용 가능한 역할 단위다.

## 8.1 Agent 1: Product Planner

역할:

- 요구사항 정리
- MVP 범위 고정
- 필드 정의
- 화면 흐름 우선순위 확정

담당 산출물:

- 요구사항 문서
- 화면 흐름 문서
- 기능 우선순위표

## 8.2 Agent 2: Android Architect

역할:

- 프로젝트 구조 설계
- 기술 스택 확정
- Room DB 구조 설계
- Navigation 구조 설계

담당 산출물:

- 아키텍처 문서
- 패키지 구조
- DB 스키마 초안

## 8.3 Agent 3: UI Builder

역할:

- Compose 기반 화면 구현
- 목록 화면 구현
- 입력 폼 구현
- 상세 화면 UX 정리

담당 산출물:

- 홈 화면
- 거래처/과/기기/PC 화면
- 입력/수정 UI

## 8.4 Agent 4: Data Flow Builder

역할:

- Entity, DAO, Repository 구현
- ViewModel 연결
- CRUD 동작 보장
- 검색 로직 연결

담당 산출물:

- Room Entity/DAO
- Repository 코드
- ViewModel 로직

## 8.5 Agent 5: Security and QA

역할:

- 비밀번호 표시 정책 검토
- 입력 검증 점검
- 삭제 동작 검증
- APK 테스트 체크리스트 작성

담당 산출물:

- QA 체크리스트
- 민감정보 처리 기준
- 버그 수정 목록

## 9. 오늘 안에 끝내기 위한 실제 실행 순서

가장 현실적인 순서는 아래다.

1. 요구사항 확정
2. DB 스키마 확정
3. 화면 흐름 확정
4. 프로젝트 생성
5. 거래처 CRUD
6. 과 CRUD
7. 기기 CRUD
8. PC CRUD
9. 검색 기능
10. UX 보강
11. 실제 기기 테스트
12. APK 빌드

## 10. 오늘 기준 성공 조건

아래가 되면 오늘 목표는 달성한 것이다.

- 거래처를 만들 수 있다.
- 거래처 안에 과를 만들 수 있다.
- 과 안에 기기를 등록할 수 있다.
- 과 안에 PC 자리 정보를 등록할 수 있다.
- IP, ID, PW를 확인할 수 있다.
- 수정과 삭제가 된다.
- 앱을 껐다 켜도 데이터가 남는다.
- APK 설치 후 실제 사용 가능하다.

## 11. 최종 결론

이번 앱은 서버 없이도 충분히 개발 가능하다.

가장 중요한 판단은 아래 3가지다.

1. 안드로이드 전용으로 고정한다.
2. `Kotlin + Jetpack Compose + Room` 조합으로 간다.
3. 오늘은 CRUD와 검색 중심 MVP까지만 완성한다.

이 방식이면 오늘 안에 "실제로 설치해서 쓸 수 있는 현장용 관리 앱 1차 버전"까지 가는 것이 현실적이다.
