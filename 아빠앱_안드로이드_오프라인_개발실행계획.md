# 아빠 앱 안드로이드 오프라인 개발 실행 계획

## 1. 전제 조건

이 앱은 아래 조건을 기준으로 개발한다.

- 플랫폼: 안드로이드 전용
- 서버: 없음
- 데이터 저장: 앱 내부 로컬 저장
- 주요 사용자: 아빠 본인
- 핵심 목적: 거래처별 과별 기기/PC 접속 정보 조회 및 관리
- 개발 목표: 오늘 안에 MVP 완성 가능한 구조로 설계 및 구현

## 2. 개발 방향 요약

서버가 없기 때문에 구조는 최대한 단순하게 가져간다.

- UI: 안드로이드 앱 화면
- 데이터: 로컬 DB(Room 권장)
- 아키텍처: 화면, 상태, DB를 분리한 단순 구조
- 기능 중심: CRUD + 검색 + 빠른 조회
- 배포: APK 직접 설치

가장 적합한 MVP 기술 방향은 다음과 같다.

- 언어: Kotlin
- UI: Jetpack Compose
- 로컬 DB: Room
- 상태 관리: ViewModel
- 네비게이션: Navigation Compose

이 조합이 적합한 이유는 다음과 같다.

- 서버 없이 로컬 앱만으로 완결 가능함
- Android 전용 MVP를 가장 빠르게 만들 수 있음
- CRUD 화면과 목록형 UI 구현이 쉬움
- 이후 기능 확장 시 유지보수가 편함

## 3. 권장 서브 에이전트 구성

이번 작업은 한 사람이 개발하더라도 역할을 나눠서 진행하면 훨씬 빠르다.

서브 에이전트는 실제 AI 도구로 나눠도 되고, 사람이 역할을 나눠 체크리스트처럼 사용해도 된다.

### Agent 1. 기획 정리 에이전트

역할:

- 요구사항 정리
- 화면 흐름 정리
- 필수/후순위 기능 구분

담당 결과물:

- 요구사항 문서
- 화면 흐름도
- MVP 범위 정의

### Agent 2. UI 설계 에이전트

역할:

- 홈 화면, 거래처 상세, 과 상세, 기기/PC 등록 화면 설계
- 입력 폼 구조 단순화
- 현장 조회에 맞는 정보 우선순위 정리

담당 결과물:

- 화면 와이어프레임
- 화면별 컴포넌트 목록
- 입력 폼 항목 정의

### Agent 3. 데이터 설계 에이전트

역할:

- Room 테이블 설계
- 관계 구조 정리
- 검색 조건 정리

담당 결과물:

- Entity 설계
- DAO 설계
- 로컬 DB 구조 문서

### Agent 4. 앱 구현 에이전트

역할:

- 프로젝트 생성
- 화면 구현
- CRUD 연결
- 검색 기능 구현

담당 결과물:

- 실행 가능한 안드로이드 앱
- 주요 화면 구현
- 로컬 저장 동작

### Agent 5. 검수/배포 에이전트

역할:

- 기능 테스트
- 입력 오류 점검
- APK 빌드 및 설치 검증

담당 결과물:

- 테스트 체크리스트
- 버그 수정 목록
- 설치 가능한 APK

## 4. 전체 개발 단계

전체 개발은 아래 순서대로 진행한다.

1. 요구사항 잠금
2. 기술 스택 확정
3. 화면 구조 설계
4. 데이터 모델링
5. 프로젝트 초기 세팅
6. 거래처 CRUD 구현
7. 과 CRUD 구현
8. 기기 CRUD 구현
9. PC CRUD 구현
10. 검색 기능 구현
11. 예외 처리 및 UX 보정
12. 테스트
13. APK 빌드 및 배포

## 5. 단계별 작업 분해

## 5.1 1단계 - 요구사항 분석 및 잠금

목적:

- 오늘 안에 만들 기능과 만들지 않을 기능을 확정한다.

주요 작업:

- 앱 목적 재확인
- 거래처 > 과 > 기기/PC 구조 확정
- 각 항목의 필드명 확정
- 오늘 안에 구현할 MVP 확정

산출물:

- 요구사항 문서
- 데이터 항목 목록
- MVP 범위 문서

완료 기준:

- 거래처, 과, 기기, PC의 필드가 확정됨
- 조회/등록/수정/삭제 범위가 확정됨
- 오늘 제외할 기능이 명확히 정리됨

예상 리스크:

- 중간에 기능이 계속 추가되면 개발 속도가 급격히 느려짐
- 기기 항목과 PC 항목 정의가 섞이면 구조가 복잡해짐

권장 담당 서브 에이전트:

- 기획 정리 에이전트

## 5.2 2단계 - 기술 스택 및 구조 확정

목적:

- 안드로이드 오프라인 앱에 맞는 최소 구조를 고정한다.

주요 작업:

- Kotlin + Jetpack Compose 사용 확정
- Room DB 사용 확정
- MVVM 수준의 단순 구조 확정
- Navigation 방식 확정

산출물:

- 기술 스택 결정 메모
- 프로젝트 폴더 구조 초안

완료 기준:

- 어떤 기술로 만들지 더 이상 고민하지 않아도 되는 상태
- DB, 화면, 상태관리 방식이 모두 결정된 상태

예상 리스크:

- 기술 선택을 오래 고민하면 개발 시작이 늦어짐
- 익숙하지 않은 기술을 너무 많이 도입하면 구현 속도가 떨어짐

권장 담당 서브 에이전트:

- 데이터 설계 에이전트
- 앱 구현 에이전트

## 5.3 3단계 - UI 구조 설계

목적:

- 사용 흐름에 맞는 화면 구조를 먼저 확정한다.

주요 작업:

- 홈 화면 구성
- 거래처 상세 화면 구성
- 과 상세 화면 구성
- 기기 등록/수정 화면 구성
- PC 등록/수정 화면 구성

권장 화면 구조:

- 홈: 거래처 목록 + 검색 + 추가 버튼
- 거래처 상세: 과 목록 + 거래처 정보
- 과 상세: 기기 탭 / PC 탭
- 등록 화면: 입력 폼 + 저장 버튼

산출물:

- 화면 목록
- 화면별 필수 요소 정의
- 화면 이동 흐름도

완료 기준:

- 각 화면에서 무엇을 보여줄지 정리됨
- 뒤로가기 및 이동 흐름이 정리됨
- 등록/수정/삭제 버튼 위치가 확정됨

예상 리스크:

- 화면 설계 없이 바로 개발하면 나중에 구조를 다시 뜯어고쳐야 함
- 상세 화면과 편집 화면이 뒤섞이면 사용성이 나빠짐

권장 담당 서브 에이전트:

- UI 설계 에이전트

## 5.4 4단계 - 데이터 모델링

목적:

- 로컬 DB 구조를 안정적으로 설계한다.

주요 작업:

- 거래처 Entity 정의
- 과 Entity 정의
- 기기 Entity 정의
- PC Entity 정의
- 관계 설정
- DAO 메서드 초안 작성

권장 테이블:

### Client

- id
- name
- address
- managerName
- phone
- memo

### Department

- id
- clientId
- name
- memo

### Device

- id
- departmentId
- deviceNumber
- modelName
- ipAddress
- loginId
- loginPassword
- memo

### PcEntry

- id
- departmentId
- seatSuffix
- ipAddress
- loginId
- loginPassword
- memo

산출물:

- Entity 코드 설계안
- DAO 목록
- DB 관계 정리 문서

완료 기준:

- 모든 CRUD가 가능한 데이터 구조가 정의됨
- 거래처 삭제 시 과/기기/PC 처리 기준이 정리됨
- 검색 대상 필드가 정리됨

예상 리스크:

- 테이블 관계를 애매하게 만들면 삭제/수정 시 오류가 남
- 기기와 PC를 한 테이블로 합치면 관리가 더 복잡해질 수 있음

권장 담당 서브 에이전트:

- 데이터 설계 에이전트

## 5.5 5단계 - 안드로이드 프로젝트 초기 세팅

목적:

- 실제 구현 가능한 기본 프로젝트를 만든다.

주요 작업:

- Android 프로젝트 생성
- Compose 설정
- Room 의존성 추가
- Navigation 의존성 추가
- 패키지 구조 생성

추천 패키지 구조:

- ui
- data
- domain(optional)
- navigation
- feature/client
- feature/department
- feature/device
- feature/pc

산출물:

- 빌드 가능한 기본 앱 프로젝트
- 기본 네비게이션 틀

완료 기준:

- 앱이 기기 또는 에뮬레이터에서 실행됨
- 빈 화면이라도 앱이 정상 구동됨
- 기본 화면 이동이 가능함

예상 리스크:

- Gradle 설정 문제로 초기 시간이 많이 소모될 수 있음
- 패키지 구조를 너무 크게 잡으면 오히려 느려짐

권장 담당 서브 에이전트:

- 앱 구현 에이전트

## 5.6 6단계 - 거래처 CRUD 구현

목적:

- 가장 상위 단위인 거래처를 완성한다.

주요 작업:

- 거래처 목록 화면
- 거래처 추가 화면
- 거래처 수정 화면
- 거래처 삭제 기능

산출물:

- 거래처 목록 UI
- 거래처 입력 폼
- Room 연동 CRUD

완료 기준:

- 거래처를 등록하면 목록에 바로 보임
- 수정 시 값이 반영됨
- 삭제 시 목록에서 제거됨

예상 리스크:

- 폼 검증이 없으면 빈 값 저장 가능
- 삭제 시 연결된 과 데이터를 어떻게 처리할지 미리 정해야 함

권장 담당 서브 에이전트:

- 앱 구현 에이전트
- 데이터 설계 에이전트

## 5.7 7단계 - 과 CRUD 구현

목적:

- 거래처 내부의 과 구조를 완성한다.

주요 작업:

- 거래처 상세에서 과 목록 표시
- 과 추가/수정/삭제
- 거래처와 과 연결

산출물:

- 과 목록 UI
- 과 입력 폼
- 거래처별 과 조회 로직

완료 기준:

- 거래처를 선택하면 그 거래처 소속 과만 보임
- 과 등록/수정/삭제가 정상 동작함

예상 리스크:

- 거래처 ID 연결 누락 시 데이터가 섞일 수 있음
- 화면 이동 구조가 꼬이면 사용자가 현재 위치를 헷갈릴 수 있음

권장 담당 서브 에이전트:

- 앱 구현 에이전트

## 5.8 8단계 - 기기 CRUD 구현

목적:

- 과별 대여 기기 관리 기능을 완성한다.

주요 작업:

- 기기 목록 표시
- 기기 추가/수정/삭제
- 기기 상세 보기
- 기기 필드 검증

산출물:

- 기기 목록 화면
- 기기 입력/상세 화면
- 기기 DB 연동

완료 기준:

- 특정 과에 등록된 기기만 보임
- IP, ID, PW가 상세에서 정상 표시됨
- 저장/수정/삭제가 정상 동작함

예상 리스크:

- 기기번호와 모델명 역할이 헷갈릴 수 있음
- 비밀번호를 평문으로 그대로 보여주는 UX에 대한 보완이 필요함

권장 담당 서브 에이전트:

- 앱 구현 에이전트
- UI 설계 에이전트

## 5.9 9단계 - PC CRUD 구현

목적:

- 과별 PC 자리 정보 관리를 완성한다.

주요 작업:

- PC 목록 표시
- PC 추가/수정/삭제
- 자리 뒷번호와 IP 매칭 정보 표시
- 상세 보기

산출물:

- PC 목록 화면
- PC 입력/상세 화면
- PC DB 연동

완료 기준:

- 과별로 PC 목록이 분리되어 보임
- 자리 뒷번호 기준으로 쉽게 찾을 수 있음
- 저장/수정/삭제가 정상 동작함

예상 리스크:

- 자리 뒷번호 중복 허용 여부를 정하지 않으면 혼선이 생길 수 있음
- 기기 목록과 PC 목록 UI가 너무 비슷하면 구분이 어려울 수 있음

권장 담당 서브 에이전트:

- 앱 구현 에이전트
- UI 설계 에이전트

## 5.10 10단계 - 검색 기능 구현

목적:

- 현장에서 빠르게 정보를 찾을 수 있도록 한다.

주요 작업:

- 거래처명 검색
- 과명 검색
- 기기번호/모델명 검색
- IP 검색
- 자리 뒷번호 검색

우선순위:

- 1순위: 거래처명 검색
- 2순위: 과 내부 기기/PC 검색
- 3순위: 전체 통합 검색

산출물:

- 검색 입력창
- 검색 결과 표시 로직

완료 기준:

- 핵심 항목 검색이 1~2초 내 즉시 반영됨
- 입력한 검색어에 맞는 결과만 남음

예상 리스크:

- 모든 화면에 검색을 한 번에 넣으면 일정이 밀릴 수 있음
- 전체 통합 검색은 MVP 범위에서 복잡도가 높을 수 있음

권장 담당 서브 에이전트:

- 데이터 설계 에이전트
- 앱 구현 에이전트

## 5.11 11단계 - UX 보정 및 예외 처리

목적:

- 실제 사용 중 생길 불편함을 줄인다.

주요 작업:

- 빈 입력값 방지
- 삭제 확인 다이얼로그
- 비밀번호 가림 처리
- 저장 성공/실패 안내
- 목록 비어 있을 때 안내 문구 표시

산출물:

- 입력 검증 로직
- 예외 처리 UI
- 경고/확인 메시지

완료 기준:

- 잘못된 입력이 저장되지 않음
- 삭제 시 실수 방지 확인이 동작함
- 비밀번호 노출 UX가 최소한 관리됨

예상 리스크:

- 예외 처리를 뒤로 미루면 마지막에 버그가 한꺼번에 생김
- 너무 많은 알림 메시지는 오히려 사용을 방해함

권장 담당 서브 에이전트:

- UI 설계 에이전트
- 앱 구현 에이전트

## 5.12 12단계 - 테스트

목적:

- APK 배포 전에 실사용 가능한지 확인한다.

주요 작업:

- CRUD 동작 테스트
- 거래처-과-기기/PC 연결 테스트
- 검색 테스트
- 삭제 시 데이터 처리 테스트
- 앱 재실행 후 데이터 유지 테스트

테스트 체크 항목:

- 거래처 등록 후 앱 재실행 시 유지되는가
- 과가 올바른 거래처 아래에 저장되는가
- 기기와 PC가 올바른 과 아래에 저장되는가
- 수정한 값이 즉시 반영되는가
- 삭제 후 목록과 DB가 일치하는가
- 검색이 정상 작동하는가

산출물:

- 테스트 체크리스트
- 발견 버그 목록
- 수정 완료 목록

완료 기준:

- 핵심 시나리오를 모두 직접 검증함
- 치명적 버그 없이 주요 기능이 모두 동작함

예상 리스크:

- 시간이 부족하면 테스트가 가장 먼저 잘릴 수 있음
- 실제 기기 테스트 없이 끝내면 배포 후 UI 문제가 발생할 수 있음

권장 담당 서브 에이전트:

- 검수/배포 에이전트

## 5.13 13단계 - APK 빌드 및 배포

목적:

- 실제 안드로이드 기기에 설치 가능한 버전을 만든다.

주요 작업:

- Debug APK 또는 Release APK 빌드
- 실제 기기 설치
- 기본 사용 검증
- 아이콘/앱 이름 최종 확인

산출물:

- 설치 가능한 APK 파일
- 설치 확인 기록

완료 기준:

- APK가 정상 설치됨
- 앱 실행 후 주요 기능이 문제없이 동작함

예상 리스크:

- 서명 설정이나 빌드 설정 문제로 APK 생성이 지연될 수 있음
- 에뮬레이터에서 되더라도 실제 기기에서 입력 UI가 다르게 보일 수 있음

권장 담당 서브 에이전트:

- 검수/배포 에이전트

## 6. 오늘 안에 끝내기 위한 우선순위

오늘 안에 완성하려면 아래 순서대로 집중해야 한다.

### 반드시 구현

- 거래처 CRUD
- 과 CRUD
- 기기 CRUD
- PC CRUD
- 로컬 저장
- 기본 검색
- APK 설치 확인

### 가능하면 구현

- 비밀번호 가림/보기 토글
- 삭제 확인 다이얼로그
- 메모 필드

### 오늘은 제외 가능

- 로그인
- 클라우드 백업
- 다중 사용자
- 이미지 첨부
- 엑셀 내보내기
- 복잡한 통계

## 7. 추천 실제 작업 순서

가장 현실적인 구현 순서는 아래와 같다.

1. 요구사항 문서 잠금
2. Room 테이블 4개 설계
3. 홈 화면 + 거래처 CRUD 구현
4. 거래처 상세 + 과 CRUD 구현
5. 과 상세 + 기기 CRUD 구현
6. 과 상세 + PC CRUD 구현
7. 검색 기능 추가
8. 입력 검증 추가
9. 실제 기기 테스트
10. APK 빌드

## 8. 개발 완료 기준

아래 조건을 만족하면 MVP 완료로 본다.

- 거래처를 만들 수 있다
- 거래처 안에 과를 만들 수 있다
- 각 과에 기기를 등록할 수 있다
- 각 과에 PC 자리 정보를 등록할 수 있다
- 각 정보의 IP, ID, PW를 확인할 수 있다
- 수정/삭제가 정상 동작한다
- 앱을 껐다 켜도 데이터가 유지된다
- APK 설치 후 실제 사용 가능하다

## 9. 가장 큰 리스크 3가지

### 1. 기능 욕심으로 범위가 커지는 것

대응:

- 오늘은 CRUD와 조회 중심으로만 끝낸다.

### 2. 로컬 DB 구조를 늦게 잡는 것

대응:

- Room Entity와 관계를 먼저 확정하고 화면 구현에 들어간다.

### 3. 테스트 없이 APK만 만드는 것

대응:

- 최소 30분은 실제 기기 테스트에 반드시 사용한다.

## 10. 최종 제안

이 앱은 서버 없이도 충분히 구현 가능하며, 오늘 안에 끝내려면 다음 전략이 가장 현실적이다.

- Android 전용으로 Kotlin + Compose + Room 조합 사용
- 거래처 > 과 > 기기/PC 구조를 고정
- CRUD와 검색만 먼저 완성
- 마지막에 APK 빌드 및 실제 기기 테스트 진행

즉, 오늘 목표는 "실사용 가능한 오프라인 현장 관리 앱 MVP"로 잡는 것이 맞다.
