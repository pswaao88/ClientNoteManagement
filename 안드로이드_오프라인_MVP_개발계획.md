# 안드로이드 오프라인 MVP 개발 계획서

## 1. 전제 조건

이 앱은 아래 조건으로 개발한다.

- 안드로이드 전용
- 서버 없음
- 오프라인 단독 실행
- 오늘 안에 MVP 완성 목표
- 주 사용자는 아빠 1명

즉, 가장 중요한 기준은 "현장에서 거래처 > 과 > 기기/PC 정보에 빠르게 들어가서 IP, ID, PW를 바로 확인하는 것"이다.

## 2. 최종 개발 방향

이번 버전은 복잡한 구조보다 안정적으로 빨리 완성되는 구조를 선택한다.

권장 기술 구조:

- 언어: Kotlin
- UI: Jetpack Compose
- 화면 이동: Navigation Compose
- 로컬 DB: Room
- 상태 관리: ViewModel + StateFlow
- 설정 저장: DataStore
- 빌드 도구: Android Studio + Gradle

이 조합을 추천하는 이유:

- 서버 없이도 CRUD 앱을 빠르게 만들 수 있다.
- Room으로 거래처, 과, 기기, PC를 안정적으로 저장할 수 있다.
- Compose로 화면 제작 속도가 빠르다.
- 유지보수가 쉽고 안드로이드 표준 구조에 가깝다.

## 3. 로컬 저장 방식 구체화

## 3.1 저장 방식 결론

MVP는 `Room(SQLite 기반)`으로 간다.

이유:

- 서버가 없으므로 앱 내부 DB가 핵심 저장소가 된다.
- 거래처, 과, 기기, PC는 관계형 데이터라 Room이 가장 잘 맞는다.
- 검색, 수정, 삭제, 정렬이 편하다.
- 오늘 안에 개발 가능한 현실적인 선택이다.

## 3.2 저장 대상

### 거래처 테이블

- id
- name
- address
- manager_name
- phone
- memo
- created_at
- updated_at

### 과 테이블

- id
- client_id
- name
- memo
- created_at
- updated_at

### 기기 테이블

- id
- department_id
- device_number
- model_name
- ip_address
- login_id
- login_password
- memo
- created_at
- updated_at

### PC 테이블

- id
- department_id
- seat_suffix
- ip_address
- login_id
- login_password
- memo
- created_at
- updated_at

## 3.3 Room 관계 설계

- 거래처 1 : N 과
- 과 1 : N 기기
- 과 1 : N PC

즉, 조회 흐름은 아래처럼 단순하게 유지한다.

`거래처 목록 -> 과 목록 -> 기기 목록 / PC 목록 -> 상세`

## 3.4 DataStore 사용 범위

DataStore는 DB 대신 쓰지 않고, 앱 설정용으로만 쓴다.

저장 예시:

- 앱 잠금 사용 여부
- 간단 PIN 사용 여부
- 최근 본 거래처 id
- 비밀번호 가리기 기본값

## 3.5 보안 관점 저장 방침

오늘 안 MVP 기준:

- 주요 데이터는 Room 저장
- 비밀번호는 상세 화면에서 기본 가림 처리
- 상세 화면은 스크린샷 제한 적용
- 앱 잠금용 PIN은 DataStore 또는 Keystore 연계로 저장

2차 개선:

- DB 암호화 도입 검토
- 내보내기 파일 암호화
- 자동 잠금 시간 설정

주의:

Room 자체는 기본적으로 암호화 DB가 아니다. 오늘 안 완성이 우선이면 MVP는 일반 Room으로 가고, 민감 정보 보호는 화면/앱 잠금 중심으로 최소 대응하는 것이 현실적이다.

## 4. 화면 구조

## 4.1 전체 화면 흐름

1. 스플래시 또는 앱 잠금 화면
2. 거래처 목록 화면
3. 거래처 상세 화면
4. 과 상세 화면
5. 기기 등록/수정 화면
6. PC 등록/수정 화면
7. 기기 상세 화면
8. PC 상세 화면

## 4.2 화면별 역할

### 거래처 목록 화면

- 거래처 리스트 표시
- 상단 검색
- 거래처 추가 버튼
- 최근 수정 순 또는 이름 순 정렬

### 거래처 상세 화면

- 거래처 기본 정보 표시
- 과 목록 표시
- 과 추가 버튼

### 과 상세 화면

- 상단에 과 이름 표시
- 탭 2개 또는 섹션 2개 구성
- `대여 기기`
- `연결 PC`

이 화면이 앱의 핵심이다.

### 기기 목록 영역

- 기기번호
- 모델명
- IP 주소
- 수정 버튼
- 삭제 버튼

### PC 목록 영역

- 자리 뒷번호
- IP 주소
- 수정 버튼
- 삭제 버튼

### 기기 상세 화면

- 기기번호
- 모델명
- IP 주소
- ID
- PW
- 비고
- 복사 버튼

### PC 상세 화면

- 자리 뒷번호
- IP 주소
- ID
- PW
- 비고
- 복사 버튼

### 등록/수정 화면

입력 필드는 최대한 단순하게 유지한다.

기기:

- 기기번호
- 모델명
- IP 주소
- ID
- PW
- 비고

PC:

- 자리 뒷번호
- IP 주소
- ID
- PW
- 비고

## 4.3 UX 원칙

- 3단계 이상 깊게 들어가지 않게 구성
- 목록에서 핵심 정보 먼저 노출
- 수정/삭제는 바로 가능하게 배치
- 비밀번호는 기본 숨김
- 복사 버튼으로 현장 사용 속도 개선

## 5. MVP 범위

## 5.1 오늘 안에 반드시 만들 기능

- 거래처 CRUD
- 과 CRUD
- 기기 CRUD
- PC CRUD
- 거래처/과/기기/PC 검색
- 상세 보기
- IP, ID, PW 복사
- 로컬 저장
- 앱 재실행 후 데이터 유지

## 5.2 오늘 안에 제외할 기능

- 회원가입/로그인
- 서버 동기화
- 사진 첨부
- 엑셀 입출력
- 클라우드 백업
- 다중 사용자 권한
- 푸시 알림

## 5.3 있으면 좋지만 후순위인 기능

- 앱 잠금 PIN
- 최근 본 거래처
- 즐겨찾기 거래처
- 비밀번호 보기 토글
- 삭제 확인 다이얼로그 개선

## 6. 추천 아키텍처

MVP는 너무 무겁지 않게 아래 구조로 간다.

### presentation

- Compose Screen
- ViewModel
- UI State

### data

- Room Entity
- DAO
- Repository
- DataStore

### domain

- 필수는 아님
- 시간이 부족하면 생략 가능

오늘 안 완성이 목표라면 `presentation + data` 중심으로 단순화해도 충분하다.

## 7. 실제 개발 단계

## 1단계. 프로젝트 생성

목표:

- Android Studio에서 새 프로젝트 생성
- Kotlin + Compose 기반 설정
- 패키지 구조 생성

완료 기준:

- 빈 화면 앱이 에뮬레이터 또는 실기기에서 실행됨

## 2단계. 데이터 구조 구현

목표:

- Room Entity 작성
- DAO 작성
- Database 클래스 작성
- Repository 작성

완료 기준:

- 거래처/과/기기/PC 데이터가 로컬 DB에 저장되고 읽힘

## 3단계. 거래처 화면 구현

목표:

- 거래처 목록
- 거래처 추가
- 거래처 수정
- 거래처 삭제
- 거래처 검색

완료 기준:

- 거래처 흐름이 완전히 동작

## 4단계. 과 화면 구현

목표:

- 거래처 상세 진입
- 과 목록 표시
- 과 추가/수정/삭제

완료 기준:

- 거래처 아래 과 구조가 완전히 동작

## 5단계. 과 상세 화면 핵심 구현

목표:

- 기기 목록 영역 구현
- PC 목록 영역 구현
- 각 목록에서 상세 진입

완료 기준:

- 한 과에서 기기와 PC를 모두 조회 가능

## 6단계. 기기/PC CRUD 구현

목표:

- 등록
- 수정
- 삭제
- 상세 보기
- 복사 버튼

완료 기준:

- 실사용 가능한 핵심 기능 완성

## 7단계. 최소 보안 적용

목표:

- 상세 화면 스크린샷 제한
- 비밀번호 기본 가림 처리
- 선택 시 노출

완료 기준:

- 민감 정보가 기본적으로 바로 노출되지 않음

## 8단계. 마감 전 안정화

목표:

- 더미 데이터 입력
- CRUD 동작 확인
- 검색 확인
- 앱 재실행 후 데이터 유지 확인
- 한글 UI 문구 정리

완료 기준:

- APK 설치 후 바로 사용 가능한 상태

## 8. 오늘 하루 개발 일정 예시

### 오전

- 프로젝트 생성
- Room 구조 작성
- 거래처/과 CRUD 완료

### 오후 초반

- 과 상세 화면
- 기기 CRUD
- PC CRUD

### 오후 후반

- 검색
- 복사 버튼
- 비밀번호 가림 처리
- 스크린샷 제한

### 마감 직전

- 더미 데이터 검수
- 실기기 설치
- 문구 수정
- APK 추출

## 9. 서브 에이전트 도입 방식

이번 작업은 한 사람이 개발하더라도 역할을 나눠서 진행하면 속도가 빨라진다. 아래처럼 서브 에이전트 개념으로 분리한다.

## Agent 1. Product Planner

역할:

- 요구사항 정리
- MVP/후순위 구분
- 화면 흐름 정의

산출물:

- 기능 목록
- 화면 목록
- 데이터 항목 목록

## Agent 2. Android Architect

역할:

- 패키지 구조 결정
- Room 스키마 설계
- Navigation 구조 설계
- ViewModel 상태 구조 결정

산출물:

- 프로젝트 구조
- Entity/DAO/Repository 설계안

## Agent 3. UI Builder

역할:

- Compose 화면 구현
- 목록/상세/등록 폼 제작
- 버튼 배치와 정보 우선순위 정리

산출물:

- 거래처 화면
- 과 화면
- 기기/PC 상세 및 편집 화면

## Agent 4. Data Flow Builder

역할:

- CRUD 연결
- 검색 연결
- 복사 기능 연결
- Room과 ViewModel 바인딩

산출물:

- 실제 동작하는 데이터 흐름

## Agent 5. Security and QA

역할:

- 비밀번호 가림 처리
- 스크린샷 제한
- 삭제 확인
- 기본 테스트

산출물:

- 최소 보안 적용
- 마감 전 체크리스트

## 10. 서브 에이전트 순서 기반 실행 플랜

1. Product Planner가 요구사항과 화면 흐름을 고정한다.
2. Android Architect가 DB와 패키지 구조를 만든다.
3. UI Builder가 거래처 > 과 > 상세 화면 골격을 만든다.
4. Data Flow Builder가 CRUD와 검색을 연결한다.
5. Security and QA가 민감 정보 노출과 동작 오류를 점검한다.

실제로는 아래처럼 병렬 진행이 가능하다.

- Architect는 DB와 내비게이션 설계
- UI Builder는 더미 데이터 기준 화면 제작
- Data Flow Builder는 Repository와 ViewModel 연결

이 방식이 오늘 안 마감에 가장 유리하다.

## 11. 배포 방식

서버가 없으므로 배포도 단순하게 간다.

권장 방식:

- Android Studio에서 release APK 생성
- 아빠 폰에 직접 설치
- 초기 더미 데이터 또는 실제 데이터 일부 수동 입력

현실적인 배포 순서:

1. debug 빌드로 기능 검수
2. 실기기 테스트
3. release APK 생성
4. 직접 설치

초기에는 Play Store 배포가 아니라 수동 설치가 더 빠르고 적합하다.

## 12. 최소 보안 대응안

오늘 안 기준 최소 대응은 아래 수준이면 충분하다.

- 상세 화면 비밀번호 기본 숨김
- 비밀번호 보기 토글
- 상세 화면 스크린샷 차단
- 앱 백그라운드 전환 시 민감 화면 최소화 고려

오늘 안에는 제외 가능하지만 2차로 강하게 추천하는 것:

- 앱 실행 PIN
- DB 암호화
- 자동 잠금
- 백업 파일 암호화

## 13. 최종 결론

이 앱은 `Kotlin + Jetpack Compose + Room + ViewModel + DataStore` 조합으로 가는 것이 가장 현실적이다.

핵심은 아래 3가지다.

1. 거래처 > 과 > 기기/PC 구조를 빠르게 탐색할 수 있어야 한다.
2. 서버 없이 Room으로 모든 데이터를 안정적으로 저장해야 한다.
3. 오늘 안 완성을 위해 MVP 범위를 강하게 유지해야 한다.

즉, 이번 버전은 "오프라인에서 거래처별 장비와 PC 접속 정보를 빠르게 조회하고 수정하는 안드로이드 현장용 관리 앱"으로 정의하면 된다.
